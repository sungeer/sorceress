
pip cache purge  # 清除缓存

pip freeze > requirements.txt
pip install -r requirements.txt

# 卸载 `pip uninstall asyncmy`

去除该文件的版本管理：`git rm --cached CLAUDE.md .claudeignore`



python -m pip install fastmcp

python -m pip install DBUtils PyMySQL python-dotenv cryptography



uvicorn server:app --host 0.0.0.0 --port 7788


